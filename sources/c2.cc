//---------------------------------------------------------------------------------------------------------------------
// c++.cc
//
// <This should be a simple terminal game>
//
//---------------------------------------------------------------------------------------------------------------------
//
#include "c++.h"
//---------------------------------------------------------------------------------------------------------------------
///
/// This is an example header comment. Here, you explain, what the
/// function does. Copypaste and adapt it!
///
/// @param numbers an array to check
/// @param length The length of the array. If this text is too long you can
///
/// @return the highest number
//
int main(void)
{
  using namespace std;
  // initscr();
  // printw("Hello World.");//the data from here is written to the imaginary window, call refersh to flush the buffer
  // raw();//specifies that each char should be taken separately.
  // noecho();//do not show the chars typed
  // refresh();
  // getch();//wait for user input
  // keypad(stdscr, TRUE);//enables arrows reading
  // endwin();//frees the memory for the curses mode
  // initscr();
  // raw();//disable line buffering
  // noecho();//do not display printed chars
  // keypad(stdscr, TRUE);
 
  // char str[] = "Press any char";
  // char in[25];
  // // int c = getch();//characters are of integer type
  // // move(10, 10);
  // // int rows,  cols = 0;
  // // getmaxyx(stdscr, rows, cols);
  // // printw("The max is: ");
  // // move((rows + strlen("The max is: ") + 4 ) / 2, cols);
  // // printw("%d %d",rows, cols);
  // // if(c == KEY_F(1))
  // //   printw("You pressed F1!!!");
  // // else
  // //   {
  // //     printw("The word in bold: ");
  // //     attron(A_BOLD);
  // //     printw("%c", c);//can be used the same as printf
  // //     attroff(A_BOLD);//set the attribute on/off
  // //   }

  // int rows, cols = 0;
  // getmaxyx(stdscr, rows, cols);
  // mvprintw(rows / 2, (cols - strlen(str) )/ 2, "%s", str);
  // refresh();
  // getstr(in);//wait for user input
  // printw("\n, You typed: %s", in); 
  // refresh();
  // addch('c' | A_BOLD | A_UNDERLINE);//A_ = attribute
  // getch();//to see the printed message
  // endwin();


  //a program reads from a file, returning 
  return 0;
}
